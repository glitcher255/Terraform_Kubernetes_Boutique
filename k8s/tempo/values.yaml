tempo:
  config:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: "0.0.0.0:4317"
          http:
            endpoint: "0.0.0.0:4318"

metricsGenerator:
  enabled: true
  storage:
    backend: local
  processor:
    serviceGraph:
      enabled: true
    spanMetrics:
      enabled: true